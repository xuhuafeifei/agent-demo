# 架构设计（模型初始化与获取）

本文档只覆盖从配置解析到拿到 `model` 对象这一段，不涉及后续 LLM 请求、流式回包和前端渲染。

## 总览

模型初始化拆成 3 层：

1. `src/agent/agent-path.ts`
2. `src/agent/model-config.ts`
3. `src/agent/pi-embedded-runner/model.ts`

目标是把“配置来源复杂、供应商多样、模型兼容差异”收敛成一个稳定接口：

- 输入：`provider + modelId +（可选）配置`
- 输出：`{ model?: Model, error?: string }`

## 1. `src/agent/agent-path.ts`

职责：统一模型相关持久化目录，避免各处手写路径。

### 核心接口

- `resolveAgentDir(): string`

### 设计规则

- 支持环境变量覆盖（便于测试、多环境部署）。
- 无覆盖时使用默认目录（例如 `~/.fgbg/agent`）。
- 返回值必须是绝对路径，供 `models.json`、`auth`、运行时缓存复用。

### 关键点

实现里会统一解析 agent 目录，并把该目录传给模型发现与认证存储，保证：

- 同一个会话上下文下，模型配置和认证来源一致。
- CLI / 服务端 / 测试场景使用同一目录解析逻辑。

## 2. `src/agent/model-config.ts`

职责：构建“可发现模型”的供应商配置，并产出最终 `models.json`（或等价内存结构）。

这层不直接返回 `model` 实例，它负责“准备好可被发现的模型定义”。

### 2.1 供应商构建（Provider Builders）

示例：

- `buildMinimaxProvider()`
- `buildMoonshotProvider()`
- `buildKimiCodeProvider()`
- `buildQwenPortalProvider()`
- `buildXiaomiProvider()`
- `buildOllamaProvider()`

每个 Provider 至少包含：

- `baseUrl`
- `api`（如 `openai-completions`、`anthropic-messages`）
- `models[]`（每个模型的 `id/name/contextWindow/maxTokens/cost/...`）
- `apiKey`（可来自环境变量、配置文件、profile）

### 2.2 隐式供应商解析（Implicit Providers）

推荐自动检测可用供应商：

- 从环境变量读取（如 `MINIMAX_API_KEY`）。
- 从本地 profile/credential store 读取。
- 对特殊 provider 做定制逻辑（如 OAuth 占位、AWS SDK 模式）。

输出为 `Record<string, ProviderConfig>`。

当前项目实现中，`resolveImplicitProviders` 已支持：

- `minimax`
- `moonshot`
- `kimi-code`
- `qwen-portal`
- `xiaomi`
- `ollama`（本地 provider，默认加入；有 key 则附带 key）

### 2.3 显式配置合并（Explicit + Implicit）

显式配置来自项目配置文件；隐式配置来自环境与本地认证。

合并策略建议：

- 默认 `merge` 模式。
- 显式配置优先覆盖隐式配置的同名字段。
- `models[]` 以 `id` 去重合并：显式优先，隐式补齐。

### 2.4 规范化（Normalization）

对最终 provider 配置做一次规范化：

- 修正 provider id 别名（如 `z.ai -> zai`、`qwen -> qwen-portal`）。
- 修正错误的 apiKey 配置形态（例如 `"${MINIMAX_API_KEY}"` 归一化为变量名/值）。
- 为缺失 apiKey 的 provider 尝试二次注入（env/profile）。
- 统一模型 id 兼容映射（例如某些 provider 的 preview id）。

### 2.5 ensureModelJson()

职责：把最终可用 provider 配置落盘到 `models.json`。

建议行为：

- 如果没有任何可用 provider，不写文件，返回 `wrote: false`。
- 写入前做内容比对，内容一致则跳过写入。
- 写文件权限收敛（目录 `700`，文件 `600`，按需）。

返回：

- `{ agentDir: string, wrote: boolean }`

## 3. `src/agent/pi-embedded-runner/model.ts`

职责：封装 pi-core/pi-coding-agent 的模型发现与选型，输出可直接用于 Agent 的 `model` 对象。

## 3.1 核心接口

- `resolveModel(provider: string, modelId: string, agentDir?: string, cfg?: Config)`

返回：

- `model?: Model`
- `error?: string`
- （可选）`authStorage`、`modelRegistry` 用于调试

## 3.2 获取 model 的标准流程

1. 解析 `agentDir`。
2. 创建认证存储（`discoverAuthStorage(agentDir)`）。
3. 创建模型注册表（`discoverModels(authStorage, agentDir)`）。
4. 先从 registry 查找：`modelRegistry.find(provider, modelId)`。
5. 命中则返回标准化后的 model。
6. 未命中则尝试 inline providers（来自配置中的 providers.models）。
7. 再未命中则走受控 fallback（仅当 provider 已配置或 mock 场景）。
8. 全部失败返回 `error: Unknown model: provider/modelId`。

## 3.3 为什么不是 if-else 堆满 server

这里采用“集中式模型解析器”思路，而不是在调用方写很多 `if (provider===...)`：

- 调用方只依赖 `resolveModel(...)` 一个入口。
- provider 扩展只改模型层，不污染业务层。
- 错误可观测性更好（统一 error 格式和调试信息）。

这本质是“策略注册 + 统一入口”，接近策略模式的工程化落地。

## 4. 配置优先级与读取路径

当前实现增加了全局用户配置文件：

- 默认路径：`~/.fgbg/fgbg.json`
- 可通过环境变量覆盖：`FGBG_CONFIG_PATH`

读取顺序（同字段覆盖优先级）为：

1. 环境变量（最高优先，例如 `MINIMAX_API_KEY`）
2. 项目配置 `config/model.json`
3. 全局配置 `fgbg.json`
4. 代码内默认值（兜底）

说明：`model-config.ts` 会先合并“全局 + 项目”，然后 API Key 解析时再由环境变量覆盖。

## 4.1 配置优先级（建议）

可采用以下优先级：

1. 运行时环境变量（最高优先）
2. 本地认证存储（profile/keychain/file）
3. 项目配置文件（默认值与结构约束）
4. 内置 provider 默认值（兜底）

说明：优先级是“值解析优先级”，不等同于“provider 是否可用”。可用性还受 `models[]` 是否存在、auth 是否完整、discover 是否成功影响。

## 5. 最小调用链（到拿到 model 为止）

```ts
const agentDir = resolveAgentDir();
await ensureModelJson(); // 准备 providers/models
const result = resolveModel("minimax", "MiniMax-M2.1", agentDir, cfg);

if (!result.model) {
  throw new Error(result.error ?? "model resolve failed");
}

// 到这里，拿到了可用于后续推理的 model 对象
const model = result.model;
```

## 6. 本项目裁剪建议

当前版本已从“单 provider 最小闭环”升级为“多 provider 隐式解析 + 统一解析入口”。

建议保留：

- 多 provider builder（至少保持当前 6 个）
- `resolveImplicitProviders` 的环境变量自动发现
- `ensureModelJson` 的 merge + 幂等写入
- `resolveModel` 的 registry 命中 + inline fallback

可以先不做：

- 多 provider OAuth 流程
- Bedrock/Copilot 这类特殊鉴权分支
- 复杂 alias/allowlist 规则





# 整体项目设计

本节定义重构后的目标结构，用于把当前臃肿的 `server.ts` 拆分为“Web 层 + Agent 运行层 + Pi 执行层”。

## 目标调用链

前端 -> `src/middleware/web-layer.ts` -> `src/agent/run.ts` -> `src/agent/pi-embedded-runner/attempt.ts`

## 分层职责

### 1) `src/server.ts`（应用装配层）

职责只保留：

- 初始化 Express、CORS、静态资源。
- 注册路由（把 `/api/chat` 等路由挂到 web layer）。
- 启动端口监听与基础日志。

不再承载：

- 模型选择细节。
- Agent 事件流解析。
- SSE 回包拼装细节。

### 2) `src/middleware/web-layer.ts`（Web 中间层）

职责：

- 接收 HTTP 请求并做参数校验。
- 建立 SSE 响应头、维护连接生命周期。
- 调用 `run.ts` 暴露的方法，把流式事件转发给前端。
- 将错误统一转换为 HTTP/SSE 错误格式。

建议对外接口：

- `createWebLayer()`：返回 Express Router，注册 `/api/chat`、`/api/history`、`/api/clear`。

### 3) `src/agent/run.ts`（Agent 运行编排层）

职责：

- 负责“每次对话请求”的统一流程编排。
- 管理模型发现与选型（复用 `selectModelForRuntime`、`ensureModelJson`）。
- 管理会话读写（读取历史、落盘、清理）。
- 收集并转发流式事件给上层回调（不直接依赖 Express）。

建议对外接口：

- `getReplyFromAgent(params)`：执行一次会话并流式返回事件。
- `getHistory()`：读取会话历史。
- `clearHistory()`：清理会话历史。
- `logRuntimePaths()`：打印全局配置和会话路径。

说明：

- 该层采用 FP 思路：仅导出函数，不创建长生命周期 runtime 对象。
- `getReplyFromAgent` 返回的是标准化事件流，不是 HTML/SSE 字符串。
- 该层可记录请求时间线（request start、LLM 首包、request end）。

### 4) `src/agent/pi-embedded-runner/attempt.ts`（Pi 执行层）

职责：

- 仅负责调用 pi-core/pi-agent-core 的执行逻辑。
- 把 Pi 原始事件映射成项目内部统一事件。
- 通过回调逐条上报流式增量，不关心 Web 协议。

建议对外接口：

- `createAgentRuntime(params)`：根据入参创建单次请求使用的轻量 Agent。
- `runEmbeddedPiAgent(params)`：执行一次 prompt，流式回调事件。

说明：

- `createAgentRuntime` 在每次请求时创建，不在进程全局缓存。
- 该层不处理路由、不处理 SSE、不处理 HTTP 状态码。

## 会话与模型初始化时机

在应用启动阶段（`server.ts`）：

- 打印运行路径：`logRuntimePaths()`

在请求阶段（`run.ts`）：

- 每次请求都执行一次 `selectModelForRuntime()` 选型。
- 读取会话历史后，调用 `attempt.ts` 的 `createAgentRuntime(params)` 创建轻量 Agent。
- 调用 `runEmbeddedPiAgent()` 执行并流式回调，结束后落盘会话。

会话文件落盘目录约定：

- `src/agent/session/*`

## 流式事件标准（项目内）

建议统一以下事件类型，web-layer 只做透传：

- `message_start`
- `message_update`（包含 `delta`）
- `message_end`
- `done`
- `error`

这样上层前端不需要关心 Pi 事件细节，只消费统一协议。

## 为什么这样拆

- `server.ts` 变薄：只负责装配和启动。
- 可测试性提升：`run.ts` 与 `attempt.ts` 可单测，不依赖 Express。
- 可替换性提升：后续若替换 pi-core，只需改 `attempt.ts`。
- 可观测性提升：请求耗时、首包耗时集中在运行层记录。



## Session 管理

在~/.fgbg/session/目录下，存在两类型文件

分别是session.json, session 元数据。{session_id}文件

session.json 的结构如下

```json
  "agent:main:main": {
    "sessionId": "1231bf97-5a58-4403-a8ec-6a095e5b93f6",
    "updatedAt": 1770111669932,
    "sessionFile": "/Users/xuhuafei/.fgbg/sessions/1231bf97-5a58-4403-a8ec-6a095e5b93f6.jsonl",
    "inputTokens": 2680,
    "outputTokens": 74,
    "totalTokens": 15443,
    "modelProvider": "minimax",
    "model": "MiniMax-M2.1",
    "contextTokens": 200000
  }
```

<sessionId>.json文件存储对话信息，数据结构大概长成这样。第一行是元数据，接下来是对话信息. cwd 是 session 的工作目录，这个你先预留

```json
{"type":"session","version":3,"id":"01f32489-debf-4976-ac54-6397daa5eeab","timestamp":"2026-02-14T07:08:00.212Z","cwd":"/Users/xuhuafei/.fgbg/workspace"}
{"type":"model_change","id":"a3f0e725","parentId":null,"timestamp":"2026-02-14T07:08:00.226Z","provider":"minimax","modelId":"MiniMax-M2.1"}
{"type":"thinking_level_change","id":"6388c384","parentId":"a3f0e725","timestamp":"2026-02-14T07:08:00.226Z","thinkingLevel":"off"}
{"type":"custom","customType":"model-snapshot","data":{"timestamp":1771052880227,"provider":"minimax","modelApi":"anthropic-messages","modelId":"MiniMax-M2.1"},"id":"b0de3c4d","parentId":"6388c384","timestamp":"2026-02-14T07:08:00.227Z"}
{"type":"message","id":"cddc95fe","parentId":"b0de3c4d","timestamp":"2026-02-14T07:08:00.241Z","message":{"role":"user","content":[{"type":"text","text":"[cron:67aa96b5-e225-4abb-8a27-d9dfb47168e5 电脑端>提醒测试] 执行 shell 命令：osascript -e 'display notification \"🌧️  记得带雨披回家！\" with title \"提醒\"'\nCurrent time: Saturday, February 14th, 2026 — 3:08 PM (Asia/Shanghai)"}],"timestamp":1771052880231}}
{"type":"message","id":"db02d633","parentId":"cddc95fe","timestamp":"2026-02-14T07:08:04.670Z","message":{"role":"assistant","content":[{"type":"thinking","thinking":"用户通过 cron 发送了一个桌面通知测试>，测试内容是提醒带雨披回家。这是一个已经执行的任务，我应该确认收到这个测试消息。","thinkingSignature":"612867c5db1bd2f1f54ba5d73c9505bba40c4fd7dffd90b6aa610790eb9ed4f8"},{"type":"text","text":"收到！桌面>通知测试成功 🎉\n\n**提醒内容：** 🌧️  记得带雨披回家！  \n**时间：** 2026-02-14 15:08 (Asia/Shanghai)\n\n测试通过，cron 通知系统运行正常。"}],"api":"anthropic-messages","provider":"minimax","model":"MiniMax-M2.1","usage":{"input":16,"output":87,"cacheRead":0,"cacheWrite":12856,"totalTokens":12959,"cost":{"input":0.00024,"output":0.00522,"cacheRead":0,"cacheWrite":0.12856,"total":0.13402}},"stopReason":"stop","timestamp":1771052880238}}
```



src/agent/session/session.ts

- 暴露 initSessionState()方法， 解析session.json信息并返回sessionInitResult

src/agent/session/session-manager-init.ts

- 暴露prepareBeforeSessionManager(), 该方法会确保session.json 文件信息存在，如果不存在则会创建。

src/agent/session/session-path.ts

- 提供session.json 路径. 路径为agent-path.ts 提供的resolveStateDir(), 加上sessions/session.json

src/agent/pre-run.ts

- 提供一个prepareBeforeGetReply 方法，选择模型，初始化 session, 然后返回



另外，不要在createAgent 了，用createAgentSession，import { createAgentSession, SessionManager, SettingsManager } from "@mariozechner/Pi-coding-agent";



在每次对话启动时，都会调用一次initSessionState()方法



调用流程图

prepareBeforeGetReply -> prepareBeforeSessionManager() [确保 session.json 和<sessionId>.jsonl 文件存在] -> initSessionState() [解析session.json 的元信息, 返回当前<sessionId>.jsonl 的路径，也是真正的SessionFile] -> SessionManager.open(<sessionId>.jsonl>), 打开对话文件信息



prepareBeforeSessionManager() , 这个方法的能力如下

- 判断session.json 是否存在，如果不存在，初始化创建。
- 检测session.json 指向的sessionFile，sessionId 是否存在，如果不存在，泽创建对应<sessionId>.jsonl 文件，这个是存储对话信息的文件
- 判断是否需要创建新的sessionId, 判断方式你就先简单判断jsonl 的大小，超过 4kb 就新session



initSessionState, 最重要的功能就是解析出session.json 的数据信息，他不负责文件的持久化或者修改，只负责读取数据

而他返回的SessionInitResult, 最重要的信息是

{sessionFile, sessionId}, sessionFile 指向的是session.jsonl



# 记忆系统

## 1. 模块设计

当前实现采用 `MemoryIndexManager` 作为门面层，负责组合监听、同步和检索。

### 1.1 模块划分

- `src/memory/memory.ts`
  - `MemoryIndexManager` 生命周期管理：`start/stop`
  - 监听管理：`chokidar` 监听 `MEMORY.md` 和 `~/.fgbg/memory/*.md`
  - 事件防抖：`1.5s debounce`
  - 会话事件入口：`onMemorySourceChanged("session", sessionFile)`
  - 对外能力：`syncAll()`、`search()`

- `src/memory/indexer.ts`
  - 同步编排层：`syncMemoryByPath`、`syncAllMemorySources`
  - 状态判定：`create/rebuild/delete/skip`
  - 文件哈希比对 + 切块 + embedding + 写库调用

- `src/memory/store.ts`
  - 存储层：`node:sqlite` + `sqlite-vec`
  - 数据表初始化与事务写入
  - FTS 查询、向量查询、按 path 删除

- `src/memory/recall.ts`
  - 检索融合层：`FTS + Vec + RRF`
  - 业务加权：`MEMORY.md +10%`、`sessions +5%`

- `src/memory/embedding/index.ts`
  - `ensureEmbeddingProviderReady()`：补齐 `fgbg.json` 中 `agents.memorySearch` 默认配置
  - `embeddingText / batchEmbeddingText`
  - 当前实现为确定性本地向量占位（384 维），后续可替换为真实模型推理

- `src/memory/utils/*`
  - 路径、hash、切块工具函数

### 1.2 数据源与触发

- 数据源：
  - `~/.fgbg/workspace/MEMORY.md`
  - `~/.fgbg/memory/*.md`
  - `sessionId.jsonl`

- 触发：
  - 文件源：`chokidar add/change/unlink`
  - 会话源：在 `src/agent/run.ts` 的对话结束 `finally` 中触发 `onMemorySourceChanged("session", prepared.sessionFile)`

### 1.3 存储模型（与代码一致）

- `files(path UNIQUE, file_hash, update_time, created_time)`
- `chunks(id, source, path, chunk_content, embedding, line_start, line_end, created_at, updated_at)`
- `chunks_fts`（FTS5 virtual table）
- `chunks_vec`（`vec0` virtual table，`embedding float[384]`）

关键约束：

- `UNIQUE(path, line_start, line_end)`
- `chunks.embedding` 与 `chunks_vec.embedding` 双存（向量冗余）
- 所有 `path` 重建流程在事务中执行

### 1.4 同步策略

`syncMemoryByPath` 规则：

- 文件不存在且库中存在：`delete`
- 文件存在且 hash 不变：`skip`
- 文件不存在于库：`create`
- 文件存在且 hash 变化：`rebuild`

单文件重建顺序：

1. 删除旧 `chunks/chunks_vec/chunks_fts`（按 path）
2. 切块（保留 `line_start/line_end`）
3. 批量 embedding
4. 写入 `chunks`（含 embedding 冗余）
5. 写入 `chunks_vec`
6. 写入 `chunks_fts`
7. 更新 `files.file_hash`

### 1.5 召回融合策略

- FTS 召回：`topK=20`
- 向量召回：`topK=20`
- RRF：`score = 1/(60 + rank_fts) + 1/(60 + rank_vec)`
- 轻量加权：`MEMORY.md +10%`，`sessions +5%`
- 最终返回：`topN=8`（默认）

### 1.6 运行约束与现状

- 依赖 `node:sqlite`，需 Node 22 环境。
- `sqlite-vec` 通过 `loadExtension(getLoadablePath())` 加载。
- 当前 `embedding` 是占位实现；文档中提到的 `node-llama-cpp` 真实推理尚未接入。
- 定时巡检任务（每 30 分钟）与 `index_meta` 重建触发目前仅为待办，代码未实现。

## 2. 接口设计

以下接口为当前代码对外暴露能力。

### 2.1 门面接口（MemoryIndexManager）

定义位置：`src/memory/memory.ts`

```ts
class MemoryIndexManager {
  start(): Promise<void>
  stop(): Promise<void>
  onMemorySourceChanged(
    source: "workspace" | "memory" | "session",
    filePath: string,
  ): void
  syncAll(): Promise<SyncSummary>
  search(query: string, options?: SearchOptions): Promise<MemoryHit[]>
}

getMemoryIndexManager(): MemoryIndexManager
```

### 2.2 同步编排接口

定义位置：`src/memory/indexer.ts`

```ts
syncMemoryByPath(params: {
  path: string;
  source?: "MEMORY.md" | "memory" | "sessions";
}): Promise<SyncResult>

syncAllMemorySources(sessionDir?: string): Promise<SyncSummary>
```

返回结构：

```ts
type SyncResult = {
  path: string;
  action: "skip" | "create" | "rebuild" | "delete";
  chunkCount: number;
  costMs: number;
};

type SyncSummary = {
  total: number;
  create: number;
  rebuild: number;
  delete: number;
  skip: number;
  failed: number;
  durationMs: number;
};
```

### 2.3 检索接口

定义位置：`src/memory/recall.ts`

```ts
searchMemory(
  query: string,
  options?: { topKFts?: number; topKVector?: number; topN?: number }
): Promise<MemoryHit[]>
```

```ts
type MemoryHit = {
  id: number;
  path: string;
  source: "MEMORY.md" | "memory" | "sessions";
  lineStart: number;
  lineEnd: number;
  content: string;
  score: number;
  scoreDetail?: {
    ftsRank?: number;
    vectorRank?: number;
    rrfScore: number;
  };
};
```

### 2.4 向量接口

定义位置：`src/memory/embedding/index.ts`

```ts
ensureEmbeddingProviderReady(): void
embeddingText(text: string): number[]
batchEmbeddingText(texts: string[]): number[][]
```

说明：当前为同步本地占位实现，后续替换为真实 embedding 服务时，接口签名保持不变。



```json
{
  "agents": {
    "memorySearch": {
      "mode": "local", // local or remote
      "model": "model name",
      "endpoint": "http:...", // 如果是 remote，才设置
      "apiKey": ""
    }
  }
}
```



# 提示词模块设计

src/agent/system-prompt.ts

对外只提供一个纯函数：

```ts
type BuildSystemPromptInput = {
  soul: string;
  user?: string;
  nowText: string;
  language: string;
  sessionMemory?: string;
  historyMemory?: string;
};

buildSystemPrompt(input: BuildSystemPromptInput): string
```

## 职责边界

- 只负责字符串模板组合（入参 -> 提示词字符串）。
- 不负责任何数据获取（不读文件、不查数据库、不做检索、不读网络）。
- 不负责长度裁剪、token 预算、记忆召回策略，这些由调用方负责。

## 当前调用方责任

调用方（`src/agent/run.ts`）负责准备输入数据，再调用 `buildSystemPrompt(input)`：

- `soul`：来自 workspace 的 `SOUL.md`
- `user`：来自 workspace 的 `USER.md`
- `nowText`：调用方生成当前时间文本
- `language`：调用方决定语言（默认 `zh-CN`）
- `sessionMemory/historyMemory`：调用方注入（当前可为空字符串）

## 模板结构

```markdown
## who you are
${soul}

## user
${user}

## environment
current date ${nowText}

## language
current language is ${language}, you need answer in ${language}

## memory recall
memory from session
${sessionMemory}

memory from history
${historyMemory}
```



# 日志系统

实现位置：`src/logger/logger.ts`

目标：替换旧日志系统，统一“文件日志 + 控制台日志 + 子系统日志”能力。

## 配置结构（`fgbg.json`）

```json
{
  "logging": {
    "cacheTime": 300,
    "level": "info",
    "file": "/tmp/fgbg/fgbg-YYYY-MM-DD.log",
    "consoleLevel": "info",
    "consoleStyle": "pretty",
    "allowModule": ["agent", "memory"]
  }
}
```

字段说明：

- `cacheTime`：配置缓存秒数，默认 `300`（5 分钟）
- `level`：写文件最低级别
- `file`：日志文件模板路径，支持 `YYYY-MM-DD`
- `consoleLevel`：控制台最低级别
- `consoleStyle`：`pretty | common | json`
- `allowModule`：子系统日志白名单（仅作用于 subsystem logger）
  - 可写字符串：`"agent,memory"`
  - 推荐数组：`["agent", "memory"]`
  - 空数组或未配置：放行所有子系统
  - 支持 `"*"`：放行所有子系统

级别枚举：

- `trace`、`debug`、`info`、`warn`、`error`、`fatal`、`silent`

## 对外接口

- `ensureLoggingSetting(): LoggingConfig`
  - 启动阶段调用（`server bootstrap`）
  - 确保 `fgbg.json.logging` 存在且字段完整
  - 缺失字段自动写回默认值
  - 同时刷新内存缓存

- `getLogger(): Logger`
  - 全局 logger，仅写文件
  - 不带子系统名

- `getSubsystemLogger(subsystem: string): Logger`
  - 子系统 logger，仅写文件
  - 带子系统名，受 `allowModule` 过滤

- `getSubsystemConsoleLogger(subsystem: string): Logger`
  - 子系统 logger，写文件 + 控制台
  - 带子系统名，受 `allowModule` 过滤

## 缓存与读取策略

- 每次打印日志先读内存缓存
- 缓存未过期：直接使用缓存配置
- 缓存过期：重新读取 `fgbg.json` 并刷新缓存
- 读取失败时降级为默认配置，不中断主流程

## 文件切分与清理

- 单日日志文件按 5MB 自动切分：
  - 主文件：`fgbg-YYYY-MM-DD.log`
  - 超过 5MB 后续写入：`fgbg-YYYY-MM-DD-1.log`、`fgbg-YYYY-MM-DD-2.log` ...
- 自动清理 7 天前日志文件：
  - 仅清理当前日志模板家族（同模板前缀）的历史文件
  - 不影响目录下其他非本系统文件

## 输出格式

- 主 logger：`[时间] [日志级别] [日志信息]`
- 子系统 logger：`[时间] [日志级别] [子系统名称] [日志信息]`

说明：子系统名称只在 `getSubsystemLogger/getSubsystemConsoleLogger` 下输出。



# 工具

本节定义 Agent 可调用工具层（`src/agent/tool`）的目标结构与行为约束。

目标是让模型侧只面对稳定的“工具契约”，并与 `pi-core`/`pi-coding-agent` 的 `AgentTool` 接口保持一致，把路径校验、文件操作、记忆检索细节收敛在工具层内部。

## 存放目录

`src/agent/tool`

建议拆分：

- `index.ts`：统一导出工具定义与注册入口
- `types.ts`：工具输入输出、错误码类型
- `guards.ts`：路径与参数校验（工作区白名单、大小限制）
- `read.ts`：读工具实现
- `write.ts`：写工具实现
- `append.ts`：append 工具实现
- `update.ts`：update 工具实现
- `memory-search.ts`：memorySearch 工具实现（复用 `MemoryIndexManager`）

## 设计目标

- 提供 5 个基础工具：`read`、`write`、`append`、`update`、`memorySearch`
- 所有文件操作默认限制在工作区内，防止越权访问
- 工具返回结构遵循 `AgentToolResult`，避免与 `pi-core` 契约偏离
- 错误可观测（明确错误码 + message），便于日志与排障
- 与现有 memory 子系统直接复用，不重复造检索逻辑

## 与 pi-core 契约对齐

`pi-core` 工具契约（简化）：

```ts
type AgentToolResult<TDetails> = {
  content: Array<{ type: "text"; text: string }>;
  details: TDetails;
};
```

每个工具实现签名应为：

```ts
execute(
  toolCallId: string,
  params: ToolInput,
  signal?: AbortSignal,
  onUpdate?: (partial: AgentToolResult<ToolDetails>) => void,
): Promise<AgentToolResult<ToolDetails>>;
```

建议统一 details 结构：

```ts
type ToolDetails<T = unknown> = {
  ok: boolean;
  data?: T;
  error?: {
    code:
      | "INVALID_ARGUMENT"
      | "PATH_OUT_OF_WORKSPACE"
      | "NOT_FOUND"
      | "ALREADY_EXISTS"
      | "CONFLICT"
      | "IO_ERROR"
      | "INTERNAL_ERROR";
    message: string;
  };
};
```

## 通用规则（所有工具）

- 所有路径先 `path.resolve` 归一化，再检查是否在 `workspace` 下
- 禁止访问 `workspace` 外路径，直接返回 `PATH_OUT_OF_WORKSPACE`
- 默认 UTF-8 文本；二进制文件不在本阶段支持范围
- 每次调用都写入工具日志（入参摘要、耗时、结果）
- 对超长输入做硬限制（例如单次写入内容上限 1MB）
- `content` 用于给模型可读反馈；结构化状态放 `details`

## read 工具

用途：读取文件内容，可按行范围返回。

入参建议：

```ts
type ReadInput = {
  path: string;
  startLine?: number; // 1-based
  endLine?: number; // 1-based，含 endLine
};
```

返回建议：

```ts
type ReadOutput = {
  path: string;
  content: string;
  lineCount: number;
};
```

`AgentToolResult` 示例：

```ts
{
  content: [{ type: "text", text: "Read 120 lines from src/a.ts" }],
  details: { ok: true, data: { path, content, lineCount } }
}
```

行为约束：

- 文件不存在返回 `NOT_FOUND`
- `startLine/endLine` 非法返回 `INVALID_ARGUMENT`
- 未传行范围时返回全文

## write 工具

用途：覆盖写文件（不存在则创建）。

入参建议：

```ts
type WriteInput = {
  path: string;
  content: string;
  createIfNotExists?: boolean; // 默认 true
  ensureParentDir?: boolean; // 默认 true
};
```

返回建议：

```ts
type WriteOutput = {
  path: string;
  bytes: number;
  created: boolean;
};
```

行为约束：

- 默认原子写：先写临时文件，再 rename 覆盖
- 父目录不存在且 `ensureParentDir=true` 时自动创建
- `createIfNotExists=false` 且文件不存在返回 `NOT_FOUND`

## append 工具

用途：向文件尾部追加文本。

入参建议：

```ts
type AppendInput = {
  path: string;
  content: string;
  ensureTrailingNewline?: boolean; // 默认 false
  createIfNotExists?: boolean; // 默认 true
};
```

返回建议：

```ts
type AppendOutput = {
  path: string;
  appendedBytes: number;
};
```

行为约束：

- 文件不存在时可按配置创建
- 当 `ensureTrailingNewline=true` 时，必要时自动补 `\n`

## update 工具

用途：按“查找并替换”方式更新文件，避免整文件重写。

入参建议：

```ts
type UpdateInput = {
  path: string;
  find: string;
  replace: string;
  all?: boolean; // 默认 false，仅替换首个匹配
  expectedCount?: number; // 期望命中次数（用于防误改）
};
```

返回建议：

```ts
type UpdateOutput = {
  path: string;
  replacedCount: number;
};
```

行为约束：

- `find` 为空返回 `INVALID_ARGUMENT`
- 未命中返回 `CONFLICT`（避免静默成功）
- 传 `expectedCount` 且实际不一致时返回 `CONFLICT`
- 写入流程与 `write` 一致，使用原子覆盖

## memorySearch 工具

用途：查询最近记忆，底层复用 `MemoryIndexManager.search()`。

入参建议（与 `SearchOptions` 对齐）：

```ts
type MemorySearchInput = {
  query: string;
  topKFts?: number;
  topKVector?: number;
  topN?: number;
};
```

返回建议：

```ts
type MemorySearchOutput = {
  query: string;
  hits: Array<{
    id: number;
    path: string;
    source: "MEMORY.md" | "memory" | "sessions";
    lineStart: number;
    lineEnd: number;
    content: string;
    score: number;
  }>;
};
```

实现要求：

- 直接调用 `getMemoryIndexManager().search(query, options)`
- 若 memory 子系统未启动，返回空 `hits`，不抛异常
- 工具层不重复实现排序逻辑，完全复用 recall 层结果

## 注册方式（pi-coding-agent）

本项目建议“内置 + 自定义”组合，避免重复造轮子：

1. 内置工具：使用 `createReadTool(cwd)`、`createWriteTool(cwd)`  
2. 自定义工具：注册 `append`、`update`、`memorySearch`  
3. 传给 `createAgentSession`：
   - `tools`: 内置工具列表
   - `customTools`: 自定义 `ToolDefinition[]`

说明：

- `append/update` 不是内置工具名，必须走 `customTools`
- 若要完全接管 `read/write` 行为，也可在 `customTools` 注册同名工具覆盖

## 与运行层集成

建议在 `src/agent/pi-embedded-runner/attempt.ts` 的 `createRuntimeAgentSession` 增加工具注入：

1. 组装 `tools`（read/write）与 `customTools`（append/update/memorySearch）
2. 调用 `createAgentSession({ ..., tools, customTools })`
3. 在 `session.subscribe` 中透传 `tool_execution_start/update/end` 事件（用于前端与日志）
4. system prompt 里 `toolings` 只保留“说明性文案”，不作为工具注册来源

## 测试建议

至少覆盖：

- 路径越权拦截（`../`、绝对路径逃逸）
- `read/write/append/update` 的正常与失败分支
- `update.expectedCount` 冲突场景
- `memorySearch` 在有索引/无索引两种状态下返回
- 并发调用下文件写入一致性（原子覆盖不产生半写入文件）
